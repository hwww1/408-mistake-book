# 408 AI 错题助手：Codex 操作说明

当用户要求在一台新的 Windows 电脑上安装或迁移本项目时：

1. 先完整阅读 `CODEX_INSTALL.md`。
2. 优先运行仓库根目录的 `安装到此电脑.cmd`；如需无界面验证，可运行 `migration/安装到此电脑.ps1 -NoShortcut -NoStart`。
3. 默认让安装程序自动选择剩余空间最大的文件系统磁盘。用户指定磁盘时，使用 `-Destination` 传入明确的绝对路径。
4. 安装前检查 Codex 是否已安装、用户是否登录了正确的 ChatGPT 账号，以及 OneDrive 是否登录了旧电脑使用的同一账号。
5. 不要复制、提交或索取 `.codex/auth.json`、GitHub 访问令牌、OneDrive 密码或浏览器登录数据。
6. 安装后验证 `http://127.0.0.1:4184/api/health` 返回成功，并确认网页能打开。
7. PDF 可以通过网页连接私有 `408-pdf-library` 仓库，也可以选择本机分节 PDF 文件夹。不要将用户的私有 PDF 改为公开。
8. 错题、错因、笔记和 AI 分析记录保存在用户的 `OneDrive/408AI错题助手数据`；不要将这些个人数据提交到本公开仓库。
9. 若用户要求完整恢复本机数据，从私有 `408-pdf-library` 仓库取得 `408AI错题助手-私人数据.zip`，运行 `migration/恢复私人错题数据.ps1 -ArchivePath <压缩包绝对路径>`。该脚本会先备份新电脑现有数据，再恢复旧电脑快照。

